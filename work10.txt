1. A variable is a name used to store a value in a program.

2. By assigning a value to a name using `=`.
age = 25

3. No. Variable names cannot start with a number.
1name = "Max"  # Error

4.  age = 25

5. Create name = "Kelechi"
name = "Kelechi"

6. Output of `x = 5; y = x; print(y)`**
5

7. type(variable)

8. `=` assigns a value.
   `==` compares two values.

9. Yes.
x = 10
x = 20

10. Variable assignment is giving a value to a variable using `=`.

11. a, b, c = 1, 2, 3

12. Output
a, b = 10, 20
print(a, b)

10 20

13. Swap `a=5, b=10` without a third variable
a, b = b, a

14. Rules for naming variables
* Must not start with a number.
* Can contain letters, numbers and `_`.
* Cannot contain spaces.
* Cannot use Python keywords.
* Variable names are case-sensitive.

15. `my_var` is valid.
    `my-var` and `my var` are invalid.

16. No. Python is case-sensitive.

17. A constant is a value that is intended not to change. Python does not have truly constant variables; uppercase names are used by convention.
PI = 3.14

18. Create PI = 3.14

PI = 3.14

19. Output
x = 10
x = 20
print(x)

20

20. How do you delete a variable?
del x

21. school = "Noun"

22. is_student = True

23. print(name _ 2)
As written, `_` is not multiplication. If the intended code is name * 2:

name = "Attah"
print(name * 2)
Output: AttahAttah

24. Fix 2nd_name = "Maxwell"

second_name = "Maxwell"

25. Fix class = "Python"
class is a Python keyword.

class_name = "Python"

26. It returns the identity/reference number of an object in memory.

x = 10
print(id(x))

27. `None` represents the absence of a value.

x = None

28. Same value to 3 variables

x = y = z = "Python"

29. What is dynamic typing?
Python determines the type of a variable automatically, and its type can change.

x = 10
x = "Hello"

30. Increase price by 50 using shorthand

price = 100
price += 50

31. What is snake_case?
Words are separated using underscores.

student_name = "Max"

32. What is camelCase?
The first word starts lowercase and subsequent words start with capital letters.

studentName = "Max"

33. Python standard for variables?
snake_case

34. What happens if `x` was never created?

print(x)

It gives a `NameError`.

35. Variables for full name, age and state

full_name = "Esho Oluwatimilehin"
age = 20
state = "Lagos"

36. a = 10
    b = 20
    a = b
    print(a)

Output: 20

37. x = "5"
    y = "10"
print(x + y)

Output: 510

38. x = 5
    y = 10
print(x + y)

Output: 15

39. a = True
print(type(a))

Output: <class 'bool'>

40. num = 10
    num = num + 5
print(num)

Output: 15

41. a = 5
    a += 3
print(a)

Output: 8

42. a = 10
    b = a
    b = 20
print(a)

Output: 10

43. Why does x, y = 5 give an error?
Because Python expects two values to assign to `x` and `y`, but only one value was provided.

Correct: x, y = 5, 10

44. first_name = "John"
    last_name = "Doe"
print(first_name + last_name)

Output: JohnDoe

45. print(first_name + " " + last_name)

Output: John Doe

46. Take user input and store it
name = input("Enter your name: ")

47. Ask for name and print Hello
name = input("Enter your name: ")
print("Hello", name)

48. Local vs global variable
A local variable is created inside a function and is normally only available there.
A global variable is created outside a function and can be accessed throughout the program.

49. Program using global
x = 10

def change():
    global x
    x = 20

change()
print(x)

50. `del` is used to delete a variable or item.

x = 10
del x

51. Can a variable store a function?
Yes.

x = print
x("Hello")

52. What is variable shadowing?
When a variable in a smaller scope has the same name as a variable in a larger scope.

53. Variable containing an apostrophe
sentence = "I'm a student"

or
sentence = 'I\'m a student'

54. How to make a variable value not changeable?
Python does not provide true constant variables. Uppercase names are used as a convention.

PI = 3.14

55. What is `_` used for?
It is commonly used for:

* A variable whose value is not important.
* Loop variables.
* Separating words in variable names (`student_name`).

56. x = 10
    y = 20
x, y = y, x
print(x, y)

Output:
20 10

57. Create a = 100 inside and outside a function
a = 50

def test():
    a = 100
    print(a)

test()
print(a)

Output: 100
        50

58. Keywords are reserved words with special meanings in Python. They cannot be used as variable names.

59. Five Python keywords
if
else
for
while
def

60. type() vs isinstance()
type() checks the exact type.
isinstance() checks whether an object belongs to a type or its subclass.

61. my_list = ["John", "Mary", "Max"]

62. data = {"name": "Max", "age": 20}

63. Explain x = y = z = 0
It assigns `0` to all three variables.

x = 0
y = 0
z = 0

64. Unpacking means assigning items from a collection to separate variables.

a, b, c = [1, 2, 3]

The question's `[1][2][3]` is not valid list syntax for three items.

65. a = [1, 2]
b = a
b.append(3)
print(a)

Output:
[1, 2, 3]

Because `a` and `b` refer to the same list.

66. How to make a copy instead of a reference?

b = a.copy()

67. What is a memory reference?
It is the reference to the object stored in memory that a variable points to.

68. Print variables created so far

print(globals())

69. x is None vs x == None
is None checks whether `x` is exactly the `None` object.
== None checks equality.
is None is the recommended way to check for None.

70. Phone number: int or string?
Use a string, because phone numbers are identifiers, not numbers used for calculations. It also preserves leading zeros and symbols.

phone_number = "08012345678"

71. Four main/common data types:
int, float, str, bool.

72. Three numeric data types:
int, float, complex.

73. int vs float
int stores whole numbers.
float stores decimal numbers.

74. str means string and stores text.

75. What is bool?
Boolean stores either:
True
False

76. 10       # int
    10.5     # float

77. "10"` → str

78. 'a' → str

79. `True`** → bool

80. [1, 2, 3] → list

81. (1, 2, 3) → tuple

82. {1, 2, 3} → set

83. {"a": 1} → dict

84. None → NoneType

85. Check data type
type(variable)

86. Convert int 10 to string
str(10)

87. Convert "10" to int
int("10")

88. int("hello")
It produces a ValueError.

89. int(10.5)
Output: 10

The decimal part is removed; it does not round up.

90. bool(0)

False

91. bool(1)

True

92. bool("")

False

93. str(100) + str(200)

Output: 100200


94. int("10.5")
No. It gives a ValueError.
Use: int(float("10.5"))

95. Convert list to tuple

tuple(my_list)

96. Three ways to create a string
"Hello"
'Hello'
"""Hello"""

97. Single vs double quotes
Both create strings. They are mostly interchangeable.

98. Multi-line string

text = """Hello
How are you?
Welcome"""

99. len("Python")

6

100. s = "Python"
s[0]

Output: P

101. Slicing extracts part of a sequence.

s[0:3]

Output: Pyt

102. s[-1]

n

103. "python".upper()`

PYTHON

104. "PYTHON".lower()`

python

105. String concatenation
Joining strings together.

"Hello" + " World"

106. Can strings be multiplied?
Yes.

"Hi" * 3

Output: HiHiHi

107. What is an f-string?
It allows variables to be inserted easily into strings.

name = "Max"
print(f"Hello {name}")

108. name = "Max"
     age = 20
print(f"My name is {name} and I am {age}")

109. What is strip()?
It removes whitespace from the beginning and end of a string.

" Hello ".strip()

110. Check if "python" is inside "I love python"

"python" in "I love python"

Output: True

111. What is a list? Is it mutable?
A list is an ordered collection of items. Yes, it is mutable.

112. What is a tuple?
A tuple is an ordered collection of items. It is immutable.

113. List vs tuple
Lists are mutable and use `[]`.
Tuples are immutable and use `()`.

114. What is a dictionary?
student = {"name": "Max", "age": 20}

It stores data as key-value pairs.

115. What is a set?
numbers = {1, 2, 3}

A set is an unordered collection of unique values.

116. Can sets have duplicates?
No.

117. Can lists have different data types?
Yes.

my_list = [10, "Hello", True]

118. Mutable vs immutable
Mutable objects can be changed.
Examples: list, dict.

Immutable objects cannot be changed after creation.
Examples: int, str.

119. a = [1, 2]
     a[0] = 99
print(a)

Output: [99, 2]

120.a = (1, 2)
    a[0] = 99

It will produce a `TypeError` because tuples cannot be changed.

**121. `[]` → list

**122. `()` → tuple

123. `{}` → dict

124. Empty set
set()

125. Empty dictionary
{}

126. type([])  # list
     type({})  # dict

127. What is indexing?
Indexing is accessing an individual item using its position. Python starts counting from `0`.

128. len([1, 2, 3, 4])

Output: 4

129. bool([])    # False
     bool([0])   # True

130. bool("")    # False
     bool(" ")   # True

131. Check if `x` is an integer

isinstance(x, int)

132. What is complex data type?
It represents numbers with real and imaginary parts.

x = 3 + 5j


133. How many data types can a list hold?
A list can hold different types at the same time.

134. Convert `[1, 2, 2, 3]` to a set

set([1, 2, 2, 3])

Output: {1, 2, 3}

The duplicate `2` is removed.

135. What is NoneType?
It is the data type of None.

136. == vs is
`==` checks whether values are equal.
`is` checks whether two variables refer to the same object.

137. Input and print its data type

x = input("Enter something: ")
print(type(x))

Input from input() is normally a string.

138. Create each data type
a = 10
b = 10.5
c = "Hello"
d = True
e = [1, 2, 3]
f = (1, 2, 3)
g = {"name": "Max"}
h = {1, 2, 3}

139.type(True)

returns: <class 'bool'>

Yes, `bool` is a subclass of `int`.

140. Why does `0.1 + 0.2 != 0.3`?
Because floating-point numbers are stored using binary representation, and some decimal numbers cannot be represented exactly. This causes a small rounding difference.

141. Seven arithmetic operators
+   -   *   /   //   %   **

142. `+` — Addition
5 + 3
# 8

143. `-` — Subtraction
5 - 3
# 2

144. `_` in the question should be `*` — Multiplication
5 * 3
# 15

145. `/` — Division
10 / 2
# 5.0

It returns a `float`.

146. `//` — Floor division
10 // 3
# 3

147. `%` — Modulus/remainder
10 % 3
# 1

148. `*` — Multiplication
2 * 3
# 6

149. `10 / 3`
3.3333333333333335

150. `10 // 3`
3

151. `10 % 3`
1

152. Operator precedence
It determines the order in which operations are performed.

`2 + 3 * 4`:
14

Multiplication happens before addition.

153. `2 * 3 * 2`
12

The multiplication operations are performed from left to right.

154. Area of rectangle
length = 10
width = 5
area = length * width
print(area)

Output: 50

155. Simple interest

P = 10000
R = 5
T = 2

SI = (P * R * T) / 100
print(SI)

Output:
1000.0

156. Six comparison operators
==   !=   >   <   >=   <=


157. `==` — Checks equality.
5 == 5
# True

158. `!=` — Not equal.
5 != 3
# True

159. `>` and `<`
`>` means greater than.
`<` means less than.

160. `>=` and `<=`
`>=` means greater than or equal to.
`<=` means less than or equal to.

161. `5 == 5.0`
True

162. `"5" == 5`
False

163. `10 > 5`
True

164. `True == 1`
True

165. `False == 0`
True

166. `and`, `or`, `not`

* `and` — both conditions must be true.
* `or` — at least one condition must be true.
* `not` — reverses the result.

167. True and False
# False

168. True or False
# True

169. not True
# False

170. 5 > 3 and 10 > 5
# True

171. 5 > 10 or 10 > 5
# True

172. Five assignment operators
=   +=   -=   *=   /=

173. `+=`
x = 5
x += 2

Same as:
x = x + 2

174. `-=`
x = 5
x -= 2

Same as:
x = x - 2

175. `_=` should be `*=`
`*=` means multiply and assign.

x = 5
x *= 2

176. `/=`
x = 10
x /= 2

177. `x += 1` is the same as

x = x + 1

178. `=` vs `+=`
`=` assigns a new value.
`+=` adds to the existing value and assigns the result.

179. What is `in`?
It checks whether a value exists inside a collection.

3 in [1, 2, 3]
# True

180. `not in`
Checks whether a value does not exist.

5 not in [1, 2, 3]
# True

181. What is `is`?
It checks whether two variables refer to the same object.

a = None
a is None
# True

182. `is` vs `==`
`==` compares values.
`is` compares object identity.

183.
a = [1, 2]
b = [1, 2]

a == b   # True
a is b   # False

184. "a" in "apple"

Output: True

185. 3 in [1, 2, 3]

Output: True

186. "key" in {"key": 1}

Output: True

187. Does `in` check values or keys in a dictionary?
It checks keys.

188. Bitwise `&` and `|`
They perform operations on binary bits.

5 & 3
# 1

5 | 3
# 7

189. Ternary operator
It is a short way of writing an `if-else` expression.

a = 10 if x > 5 else 20

190. Even/odd using ternary

num = int(input("Enter a number: "))
result = "Even" if num % 2 == 0 else "Odd"
print(result)


191. Operator chaining

1 < 2 < 3

Output: True

It is equivalent to:

1 < 2 and 2 < 3

192. `10 + 20 * 30 / 10`

70.0

193. How to force precedence?
Use parentheses:

(2 + 3) * 4

194. Take two numbers and print arithmetic operations

a = float(input("Enter first number: "))
b = float(input("Enter second number: "))

print("Addition:", a + b)
print("Subtraction:", a - b)
print("Multiplication:", a * b)
print("Division:", a / b)
print("Modulus:", a % b)
print("Floor Division:", a // b)

195. Check if age is at least 18

age = int(input("Enter your age: "))

if age >= 18:
    print("Age is 18 or above")
else:
    print("Age is below 18")

196. Divisible by both 3 and 5

num = int(input("Enter a number: "))

if num % 3 == 0 and num % 5 == 0:
    print("Divisible by both")
else:
    print("Not divisible by both")

197. Check if letter is a vowel
letter = input("Enter a letter: ")

if letter.lower() in "aeiou":
    print("Vowel")
else:
    print("Not a vowel")


198. Double a number 3 times using assignment

num = 5

num *= 2
num *= 2
num *= 2

print(num)

Output: 40

199. Explain BODMAS in Python
BODMAS describes the order of mathematical operations:

* **B** — Brackets `()`
* **O** — Orders/exponents `**`
* **D** — Division `/`
* **M** — Multiplication `*`
* **A** — Addition `+`
* **S** — Subtraction `-`

Python also follows precedence rules where multiplication/division are done before addition/subtraction.

200. Small calculator using arithmetic operators

a = float(input("Enter first number: "))
b = float(input("Enter second number: "))

print("Addition:", a + b)
print("Subtraction:", a - b)
print("Multiplication:", a * b)
print("Division:", a / b)
print("Floor Division:", a // b)
print("Modulus:", a % b)
print("Power:", a ** b)

201. Create `user_name`, take input and print in capital letters

user_name = input("Enter your name: ")
print(user_name.upper())

202. What happens?

x = 10
del x
print(x)

It gives a `NameError` because `x` was deleted before it was printed.

203. Can `a = 10` and `b = "10"` be added?
No.

a = 10
b = "10"

a + b

This gives a `TypeError` because `a` is an `int` and `b` is a `str`.

204. Swap `a = "Python"` and `b = "Java"` in one line

a = "Python"
b = "Java"

a, b = b, a

205. Output

a, b, c = 5, 10, 15
print(a + b + c)

Output: 30

206. Find the memory address of `x = 50`

x = 50
print(id(x))

207. Create `full_name` using f-string

first = "Attah"
last = "Maxwell"

full_name = f"{first} {last}"
print(full_name)

208. Which variable names are valid?

__name     Valid
*age       Valid
age*       Valid
age2       Valid
2age       Invalid

209. Output

x = 5
x = x + x
x = x + x
print(x)

Output:

20

210. Create `count = 0` and increase it to 10 using a loop

count = 0

for i in range(10):
count += 1

print(count)

Output:

10

211. Why don't Python variables need data type declaration?

Python uses dynamic typing. Python automatically determines the data type from the value assigned to the variable.

212. What happens?

data = [1, 2, 3]
data2 = data
data2[0] = 99

print(data)

Output:

[99, 2, 3]

This happens because `data` and `data2` refer to the same list.

213. How do you make an independent copy?

data2 = data.copy()

214. Store 3 student names in different variables and print them as a list

student1 = "Max"
student2 = "John"
student3 = "Sarah"

students = [student1, student2, student3]
print(students)

215. What are `globals()` and `locals()`?

`globals()` returns the global variables and their values.

`locals()` returns the local variables and their values.

216. Create variable `student_maths_score_2024`

student_maths_score_2024 = 85

217. Is this valid?

x = 10
y = 20

print(x, y) if x > y else print(y, x)

Yes, it is valid.

Output:

20 10

218. Print which variable is bigger using `result`

a = 10
b = 20

if a > b:
result = a
else:
result = b

print(result)

Output:

20

219. Can a variable name contain an emoji?

No, emojis cannot normally be used as Python identifiers.

For example:

😀 = 10

This gives a `SyntaxError`.

220. What is unpacking with `_`?

The `_` can be used for a value that we do not need.

a, _, c = [1, 2, 3]

print(a)
print(c)

Output:

1
3

The question's `[1][2][3][4][5]` should be `[1, 2, 3, 4, 5]`.

221. Create `total`

price = 19.99
qty = 3

total = price * qty
print(total)

Output:

59.97

The question's `price_qty` should be `price * qty`.

222. Take name and age as input

name = input("Enter your name: ")
age = int(input("Enter your age: "))

print(name)
print(age)

223. Difference between `x = 10` and `x = int(10)`

x = 10

Directly assigns the integer `10`.

x = int(10)

Converts `10` to an integer before assigning it.

Both produce an integer with the value `10`.

224. Toggle `is_logged_in` using `not`

is_logged_in = False

is_logged_in = not is_logged_in

print(is_logged_in)

Output:

True

225. Output

a = None
print(a is None)

Output:

True

226. Local and global `x`

x = 10

def my_func():
x = 5
print(x)

my_func()
print(x)

Output:

5
10

227. How to make a variable constant-like?

Use uppercase letters by convention.

PI = 3.14

Python does not have true constant variables.

228. Count characters in `"NOUN"`

text = "NOUN"

length = len(text)

print(length)

Output:

4

229. Output

name = "Kelechi"
name = name + " Noun"
print(name)

Output:

Kelechi Noun

230. Why does `"C:\new\folder"` cause a problem?

Because `\n` is treated as a newline escape character.

Use a raw string:

file_path = r"C:\new\folder"

Or use double backslashes:

file_path = "C:\new\folder"

DATA TYPES — Logic & Bug Finding (231–270)

231. Output of `type(1/2)`

type(1 / 2)

Output:

<class 'float'>

Division `/` returns a `float`.

232. Output of `type(2//2)`

type(2 // 2)

Output:

<class 'int'>

233. Is `"True"` the same as `True`?

No.

"True" is a string.

True is a boolean.

234. Convert `True` and `False` to integers

int(True)

# 1

int(False)

# 0

235. Output

str(True) + str(False)

Output:

TrueFalse

236. Output

bool("False")

Output:

True

A non-empty string is considered `True`, even when the text inside it says `"False"`.

237. Two ways to check if a string is empty

text = ""

len(text) == 0

or

text == ""

238. Remove spaces, uppercase and replace `"easy"` with `"powerful"`

s = " python is easy "

s = s.strip()
s = s.upper()
s = s.replace("EASY", "POWERFUL")

print(s)

Output:

PYTHON IS POWERFUL

239. Output

"python"[100]

It gives an `IndexError` because index 100 does not exist.

240. Output

"python"[1:100]

Output:

ython

It does not give an error. Python stops at the end of the string.

241. What is immutable?

Immutable means an object cannot be changed after it is created.

Strings are immutable.

"python"[0] = "P"

This gives a `TypeError`.

242. Reverse `"python"` without `reverse()`

s = "python"

print(s[::-1])

Output:

nohtyp

243. Output

len(" ")

# 1

len("")

# 0

A space is still a character.

244. Add 5 to `age = "20"`

age = "20"

age = int(age)
age = age + 5

print(age)

Output:

25

245. Difference between `list("abc")` and `["abc"]`

list("abc")

Output:

['a', 'b', 'c']

["abc"]

Output:

['abc']

246. Output of `[1, 2, 3] * 2`

[1, 2, 3] * 2

Output:

[1, 2, 3, 1, 2, 3]

The `_` in the question should be `*`.

247. Output of `(1, 2, 3) * 2`

(1, 2, 3) * 2

Output:

(1, 2, 3, 1, 2, 3)

248. What is `{"a":1} * 2`?

It will give a `TypeError`.

Dictionaries cannot be multiplied by an integer.

249. Can a dictionary have a list or tuple as a key?

A list cannot be a dictionary key because lists are mutable.

A tuple can be a dictionary key if its contents are hashable.

Example:

d = {(1, 2): "Hello"}

250. Output

set([1, 1, 2, 2, 3])

Output:

{1, 2, 3}

Duplicates are removed.

251. How do you add an item to a set?

Use `add()`.

numbers = {1, 2}
numbers.add(3)

252. How do you add an item to a list?

Use `append()`.

numbers = [1, 2]
numbers.append(3)

253. What is the common mistake?

[1, 2, 3].append(4)

`append()` changes the list but returns `None`.

Correct:

my_list = [1, 2, 3]
my_list.append(4)

print(my_list)

Output:

[1, 2, 3, 4]

254. Output

a = [1, 2]
b = a
b = [3, 4]

print(a)

Output:

[1, 2]

`b = [3, 4]` makes `b` refer to a new list. It does not change `a`.

255. Convert tuple to list, add 4, convert back

my_tuple = (1, 2, 3)

my_list = list(my_tuple)
my_list.append(4)

my_tuple = tuple(my_list)

print(my_tuple)

Output:

(1, 2, 3, 4)

256. Extract keys and values

d = {"name": "Max", "dept": "NOUN"}

keys = d.keys()
values = d.values()

print(keys)
print(values)

257. `isinstance()` vs `type()`

isinstance([1, 2], list)

# True

type([1, 2]) == list

# True

`isinstance()` is generally more flexible because it also recognizes subclasses.

258. What is `None` used for?

`None` represents no value or no result.

Example 1:

result = None

Example 2:

def greet():
print("Hello")

answer = greet()

259. Output

float("inf")

Output:

inf

Its type is:

<class 'float'>

260. Output

int(True) + int(False) + int(True)

Output:

2

261. Check whether `x` is int, float or other

x = 10.5

if isinstance(x, int):
print("Integer")
elif isinstance(x, float):
print("Float")
else:
print("Other")

Output:

Float

262. Deep copy vs shallow copy

A shallow copy copies the outer object but may still share nested objects.

A deep copy creates independent copies of nested objects too.

Use `copy.copy()` for a shallow copy.

Use `copy.deepcopy()` for a deep copy.

Example:

import copy

b = copy.copy(a)

c = copy.deepcopy(a)

263. Output

a = "5"
b = int(a)
c = float(b)

print(c)

Output:

5.0

264. List containing 5 different data types

my_list = [10, 2.5, "Hello", True, [1, 2]]

265. Output

bool([0])

Output:

True

The list is not empty. The value inside the list does not matter for the list's truth value.

266. `bytes` and `bytearray`

`bytes` is an immutable sequence of bytes.

`bytearray` is a mutable sequence of bytes.

267. Convert `123` to `"123"` and `"123"` to `123.0`

a = str(123)

b = float("123")

268. What happens?

x = ["a", "b", "c"]
print(x[0][1])

Output:

IndexError

`x[0]` is `"a"`, and `"a"` has no index `1`.

The question's `["a"]["b"]["c"]` should be `["a", "b", "c"]`.

269. What is `complex(2,3)`?

complex(2, 3)

Output:

(2+3j)

It creates a complex number with real part `2` and imaginary part `3`.

270. Take user input and detect its type

value = input("Enter something: ")

try:
value = int(value)
print(type(value))
except ValueError:
print(type(value))

Note: `input()` initially gives a string. This code attempts to convert numeric input to an integer.

OPERATORS — Calculation & Logic (271–300)

271. Output of `2 * 3 * 2`

2 * 3 = 6

6 * 2 = 12

Output:

12

272. Output of `-3 * 2`

-3 * 2

Output:

-6

The question says `_` but it should be `*`.

273. Output of `(-3) ** 2`

(-3) ** 2

Output:

9

The question's `__` should be `**`.

274. Output of `10 + 3 * 2 * 2`

10 + 3 * 2 * 2

3 * 2 = 6

6 * 2 = 12

10 + 12 = 22

Output:

22

275. Calculate BMI

weight = float(input("Enter weight: "))
height = float(input("Enter height: "))

BMI = weight / (height ** 2)

print(BMI)

The question's `__2` should be `** 2`.

276. Check if `x = 15` is between 10 and 20

x = 15

if 10 <= x <= 20:
print("Between 10 and 20")
else:
print("Not between 10 and 20")

277. Output

5 == 5 and 10 == 10 and 15 == 20

Output:

False

The last condition is false.

278. Output

5 == 5 or 10 == 20 or 15 == 20

Output:

True

At least one condition is true.

279. Output

not (5 > 3)

Output:

False

280. Output

not 0

# True

not 1

# False

not ""

# True

281. Short-circuit

False and (10 / 0)

Output:

False

It does not give a division-by-zero error because `and` stops when the first condition is already `False`.

282. Short-circuit

True or (10 / 0)

Output:

True

It does not give an error because `or` stops when the first condition is already `True`.

283. Output

10 % 2 == 0 and 10 % 3 == 0

Output:

False

For 15:

15 % 2 == 0 and 15 % 3 == 0

Output:

False

15 is divisible by 3 but not by 2.

284. Check if a number is divisible by 2

num = int(input("Enter a number: "))

if num % 2 == 0:
print("Divisible by 2")
else:
print("Not divisible by 2")

285. Check if number is even AND greater than 10

num = int(input("Enter a number: "))

if num % 2 == 0 and num > 10:
print("Even and greater than 10")
else:
print("Condition not met")

286. Output

a = 5
a += 2 * 3
print(a)

Output:

11

Because:

2 * 3 = 6

5 + 6 = 11

287. Output

x = 10
x //= 3
print(x)

Output:

3

288. Output

x = 2
x *= 3
x *= 2
print(x)

Output:

12

289. Difference between `x = x + 1` and `x += 1`

Both increase `x` by 1.

x = x + 1

and

x += 1

produce the same result.

290. Output

"ab" in "abc" and "d" not in "abc"

Output:

True

291. Check if user-entered letter is in `"python"`

letter = input("Enter a letter: ")

if letter in "python":
print("Letter is in python")
else:
print("Letter is not in python")

292. Output

a = 1000
b = 1000

a is b

The result is not something you should rely on for ordinary integers; object identity can depend on Python implementation and optimization.

For:

a = 10
b = 10

a is b

This commonly returns:

True

Small integers are commonly cached.

Use `==` when comparing values, not `is`.

293. Output

[] == []

True

[] is []

False

The two empty lists have equal values but are different objects.

294. Check if 3 variables have the same value

a = 10
b = 10
c = 10

if a == b and b == c:
print("All are the same")
else:
print("They are different")

295. What is the Walrus operator `:=`?

The walrus operator assigns a value to a variable while also using that value in an expression.

Example:

if (n := len([1, 2, 3])) > 2:
print(n)

Output:

3

296. Use walrus operator to take input until `"exit"`

while (text := input("Enter something: ")) != "exit":
print(text)

297. Output of `10 & 2`

10 & 2

Output:

2

Binary:

10 = 1010
2  = 0010

AND:

1010
0010
----

0010

So the answer is `2`.

298. Output of `10 | 2`

10 | 2

Output:

10

Binary:

1010
0010
----

1010

So the answer is `10`.

299. Swap two numbers using XOR

a = 5
b = 10

a = a ^ b
b = a ^ b
a = a ^ b

print(a, b)

Output:
10 5

300. Mini calculator using arithmetic operators
a = float(input("Enter first number: "))
b = float(input("Enter second number: "))
operator = input("Enter operator (+, -, *, /, %, //, **): ")

if operator == "+":
print(a + b)

elif operator == "-":
print(a - b)

elif operator == "*":
print(a * b)

elif operator == "/":
print(a / b)

elif operator == "%":
print(a % b)

elif operator == "//":
print(a // b)

elif operator == "**":
print(a ** b)

else:
print("Invalid operator")

301. Output:
     x = 5
     print(x)
     x = "hello"
     print(x)

Output:
5
hello

Python is strongly typed because a variable cannot be treated as an unrelated type without conversion. But Python is dynamically typed because the variable can be reassigned to another type.

302. Fix the bug:
     name = input("Enter name: ")
     age = int(input("Enter age: "))
     print(age + 10)

303. `x` is not created because the `if False` block never runs.

print(x)

Output:
NameError

304. Create 5 student variables:
     name = "John"
     matric_no = "KDU/SE/001"
     course = "Software Engineering"
     gpa = 4.20
     is_active = True

305. Output:
     a = b = c = [1]
     c.append(2)
     print(a, b, c)

Output:
[1, 2] [1, 2] [1, 2]

Surprise: all three variables refer to the same list.

306. Avoid the bug by creating separate lists:
     a = [1]
     b = [1]
     c = [1]

307.

NameError — using a variable that does not exist.

TypeError — using a variable with an inappropriate data type.

ValueError — the type is correct, but the value cannot be converted or used correctly.

308. Crash with NameError:
     print(age)

Fix:
age = 20
print(age)

309. Global variable:
     x = 10

def show():
print(x)

show()
print(x)

Output:
10
10

Local variable:
x = 10

def show():
x = 5
print(x)

show()
print(x)

Output:
5
10

310.

x = 10

def change():
global x
x = 20

change()
print(x)

Output:
20

311. `nonlocal` allows an inner function to change a variable belonging to its outer function.

def outer():
x = 10

```
def inner():
    nonlocal x
    x = 20

inner()
print(x)
```

outer()

Output:
20

312. Yes:
     a = b = c = 10

It is valid, but use it only when all variables should intentionally have the same value.

313. `_` is commonly used for a value we do not need.

for _ in range(5):
print("Hello")

The loop runs 5 times.

314.

first_name = "John"
last_name = "Doe"

full_name = first_name + " " + last_name

full_name = " ".join([first_name, last_name])

full_name = f"{first_name} {last_name}"

315.

sentence = "I love Python"

words = sentence.split()
count = 0

for word in words:
count += 1

print(count)

Output:
3

316.

x = 10
y = "10"

print(x is int(y))

Output:
False

`int(y)` creates an integer with value 10, but `is` checks whether they are the same object, not whether their values are equal.

317.

name = input("Name: ")
age = input("Age: ")
phone = input("Phone: ")

Phone should be a string because we do not perform arithmetic with phone numbers, and they may contain leading zeros or symbols such as `+234`.

318. `PI = 3.14` is only a naming convention. Python does not prevent it from changing.

PI = 3.14
PI = 4

Python has no built-in true constant variables.

319.

a = 5
b = 5
print(id(a) == id(b))

Output:
True

Python commonly reuses the same object for small integers.

320.

a = 257
b = 257
print(a is b)

The result can differ depending on how Python creates/stores the integers. In a script, it may be True because of compiler interning; in IDLE or other contexts it may be False.

Use `==` to compare values, not `is`.

321.

a = 10
b = 20
c = 30

del a, b, c

322.

print(globals().keys())

This shows names stored in the global namespace, including names created by Python.

323.

str = "hello"
print(str(10))

Output:
TypeError

The variable `str` has replaced access to Python's built-in `str()` function.

324. Q323 is dangerous because you can no longer use the built-in `str()` function normally.

325.

x = [1, 2, 3]

y = x.copy()
y.append(4)

print(x)
print(y)

Output:
[1, 2, 3]
[1, 2, 3, 4]

But:

y = x
y.append(4)

Now both refer to the same list.

326.

balance = 1000

for _ in range(3):
balance -= 100

print(balance)

Output:
700

327. Dynamic variable creation means creating variable names programmatically.

globals()["x" + str(1)] = 10

print(x1)

Output:
10

328. Usually avoid dynamic variables because they make programs harder to read, debug and maintain. A dictionary is usually better.

data = {}
data["x1"] = 10

329.

a, b, c = 1, 2, 3
a, b, c = b, c, a

Output:
a = 2
b = 3
c = 1

330.

x = True
x = x + 5
print(x)

Output:
6

`True` behaves like `1` in arithmetic.

331.

data = None

if data is not None:
print("Data has been assigned")
else:
print("No data")

332.

a = 10
b = 5

sum_result = a + b
difference = a - b
product = a * b

333. Stack and heap in simple English:

Stack — stores information related to function calls and local variables.

Heap — stores objects such as lists, dictionaries and other Python objects.

Python manages this memory automatically.

334.

name = " noun "

clean_name = name.strip().lower().capitalize()

print(clean_name)

Output:
Noun

335. Example for the name "John":
     j = "J"
     o = "o"
     h = "h"
     n = "n"

name = "".join([j, o, h, n])

print(name)

Output:
John

DATA TYPES — Deep Dive (336–390)

336.

print(type([]))
print(type(()))
print(type({}))
print(type(set()))

Output:
<class 'list'>
<class 'tuple'>
<class 'dict'>
<class 'set'>

337. `{}` is reserved for an empty dictionary.

Empty set:
s = set()

338.

print(bool(0))
print(bool(0.0))
print(bool(0j))
print(bool(""))
print(bool([]))
print(bool({}))
print(bool(None))

Output:
False
False
False
False
False
False
False

339. Seven common falsy values:

0
0.0
0j
""
[]
{}
None

340. Three surprising truthy values:

bool("False") → True
bool("0") → True
bool([0]) → True

They are truthy because they are non-empty.

341. Assuming `_` means `*`:

"hello" * 0

Output:
""

"hello" * -5

Output:
""

342.

[1, 2] * 2.5

Output:
TypeError

Lists can only be repeated using an integer.

343.

s = "Python"
print(s[::-1])

Output:
nohtyP

`[::-1]` reverses the string.

344.

s = "Python Programming"

print(s[:3])
print(s[-3:])

Output:
Pyt
ing

345. `ord()` converts a character to its Unicode number.

`chr()` converts a Unicode number back to a character.

print(ord("A"))
print(chr(65))

Output:
65
A

346.

text = input("Enter a word: ")

if text == text[::-1]:
print("Palindrome")
else:
print("Not palindrome")

347. `strip()` removes spaces from both ends.

text = "  Python  "
print(text.strip())

`lstrip()` removes from the left.

print(text.lstrip())

`rstrip()` removes from the right.

print(text.rstrip())

348. `split()` turns a string into a list.

text = "a,b,c"
parts = text.split(",")

Output:
['a', 'b', 'c']

`join()` combines strings.

result = "-".join(parts)

Output:
a-b-c

349.

language = "Python"
text = "I love Java"

text = text.replace("Java", language)

print(text)

Output:
I love Python

350.

s = "python"
before = id(s)

s += "!"

after = id(s)

print(before)
print(after)

The IDs are normally different because strings are immutable. `s += "!"` creates a new string.

351.

a = [1, 2]

a.append([4, 5])
print(a)

Output:
[1, 2, [4, 5]]

`append()` adds the entire list as one item.

a = [1, 2]
a.extend([4, 5])
print(a)

Output:
[1, 2, 4, 5]

`extend()` adds each item separately.

352.

a = [1, 2, 3]
print(a.append(4))

Output:
None

`append()` changes the list but does not return the updated list.

353.

a = [2, 3, 4]
a.insert(0, 1)

print(a)

Output:
[1, 2, 3, 4]

354. Remove by value:
     a.remove(2)

Remove by index:
a.pop(1)

`remove()` searches for a value.

`pop()` removes an item using its index and can return that item.

355.

a = [1, 2, 3]
a.pop()
a.pop(0)

print(a)

Output:
[2]

356. Yes. A tuple can contain a mutable list.

t = ([1, 2], 3)

The tuple itself cannot have its items replaced, but the list inside it can be changed.

357.

t = ([1, 2], 3)
t[0].append(99)

print(t)

Output:
([1, 2, 99], 3)

No error occurs because the list is mutable.

358.

(5)

is an integer, not a tuple.

359. A single-element tuple needs a comma:

t = (5,)

print(type(t))

Output:
<class 'tuple'>

360. A set stores unique values.

s = {[1, 2]}

This gives a TypeError because lists are unhashable.

This works:

s = {(1, 2)}

Because a tuple containing hashable values can be used in a set.

361. Hashable means an object can be used as a dictionary key or set element.

Lists are mutable, so they are not hashable.

362.

d = {True: "yes", 1: "no", 1.0: "maybe"}

print(d)

Output:
{True: 'maybe'}

`True`, `1`, and `1.0` are considered equal as dictionary keys, so the later values replace the earlier values.

363.

d = {"a": 1}

print(d["b"])

This gives:
KeyError

print(d.get("b"))

Output:
None

print(d.get("b", 0))

Output:
0

364.

d1 = {"a": 1}
d2 = {"b": 2}

d3 = d1 | d2

print(d3)

Output:
{'a': 1, 'b': 2}

365.

keys() — returns dictionary keys.
values() — returns dictionary values.
items() — returns key-value pairs.

They return dictionary view objects.

366.

text = "123,456,789"

numbers = [int(x) for x in text.split(",")]

print(numbers)

Output:
[123, 456, 789]

367.

numbers = [1, 2, 3]

result = "-".join(map(str, numbers))

print(result)

Output:
1-2-3

368.

result = list(map(int, ["1", "2", "3"]))

print(result)

Output:
[1, 2, 3]

Data type:
list

369.

mixed = [1, "a", 2.5, True, None, [1]]

for item in mixed:
print(item, type(item))

370.

print(isinstance(True, int))

Output:
True

`bool` is a subclass of `int`, so `True` behaves like `1` and `False` like `0` in some contexts.

371. `type()` checks the exact type.

`isinstance()` also considers inheritance.

Example:
isinstance(True, int)

Output:
True

For inheritance checks, `isinstance()` is generally better.

372.

value = input("Enter something: ")

if value.isdigit():
value = int(value)

print(value)
print(type(value))

373. Access 80:

data = {
"students": [
{
"name": "Max",
"scores": [90, 80]
}
]
}

print(data["students"][0]["scores"][1])

Output:
80

374. `copy()` makes a shallow copy. Nested objects can still be shared.

`deepcopy()` copies nested objects too.

from copy import deepcopy

new_data = deepcopy(old_data)

375.

a = float("nan")

print(a == a)

Output:
False

NaN means "Not a Number", and NaN is not equal to itself.

376.

import math

x = float("nan")

print(math.isnan(x))

Output:
True

377. They are different values:

None — represents no value.
0 — integer zero.
"" — empty string.
False — Boolean false.

They can all be falsy, but they are not the same thing.

378.

numbers = [1, 1, 2, 2, 3, 3]

result = list(set(numbers))

A set removes duplicates, but you should not rely on a set to preserve the original order.

379.

numbers = [1, 1, 2, 2, 3, 3]

result = list(dict.fromkeys(numbers))

print(result)

Output:
[1, 2, 3]

380.

print(len([[]]))
print(len([]))

Output:
1
0

`[[]]` contains one item: an empty list.

381. `b"hello"` is a bytes object.

It is used when working with binary data such as files, network data and encoded text.

382.

sentence = input("Enter sentence: ")
words = sentence.split()

int_count = 0
float_count = 0
string_count = 0

for word in words:
try:
int(word)
int_count += 1
except ValueError:
try:
float(word)
float_count += 1
except ValueError:
string_count += 1

print("Integers:", int_count)
print("Floats:", float_count)
print("Strings:", string_count)

383. `sys.getsizeof()` shows the memory size of an object in bytes.

import sys

x = [1, 2, 3]
print(sys.getsizeof(x))

The exact size depends on the Python implementation and object.

384.

import sys

x = 10

print("Value:", x)
print("Type:", type(x))
print("ID:", id(x))
print("Size:", sys.getsizeof(x))

385. There is no general built-in function that directly tells whether every object is mutable. A simple demonstration can be made by trying to modify it.

For example:
x = [1, 2]

try:
x.append(3)
print("Mutable")
except AttributeError:
print("Immutable")

386.

a = "python"

print(a is "python")

It may output:
True

But this is not a reliable way to compare strings.

Use:
a == "python"

`is` checks object identity; `==` checks value equality.

387. String interning is Python's optimization of storing/reusing some identical string objects.

388.

student = {
"name": "John",
"age": 20,
"courses": ["Python", "Database"],
"grades": {
"Python": 85,
"Database": 78
}
}

389. Use the `json` module.

import json

student_json = json.dumps(student)

print(student_json)

390.

value = input("Enter a value: ")

print("Value:", value)
print("Type:", type(value))
print("ID:", id(value))
print("Bool:", bool(value))

Strings are immutable, so the input value itself is not mutable.

OPERATORS — Tricky & Projects (391–450)

391. Assuming `_` means `*`:

5 + 2 * 3 * 2 / 2 - 1

Output:
10

392.

100 / 10 / 2

Output:
5.0

Division works from left to right.

393.

100 // 10 // 2

Output:
5

Floor division also works from left to right.

394.

2 * 3 * 2
(2 * 3) * 2

Both output:
12

Multiplication is evaluated left to right.

395. Compound interest formula:

A = P(1 + r/n)^(nt)

Example:
P = 10000
r = 0.05
n = 1
t = 2

A = P * (1 + r / n) ** (n * t)

print(A)

396. `/` gives normal division.

-10 / 3

Output:
-3.3333333333333335

`//` gives floor division.

-10 // 3

Output:
-4

It rounds down toward negative infinity, not simply toward zero.

397.

print(10 % -3)
print(-10 % 3)

Output:
-2
2

Python's modulo result follows the sign of the divisor.

398.

print(divmod(10, 3))

Output:
(3, 1)

It gives:
quotient = 10 // 3
remainder = 10 % 3

399.

number = 1234

d1 = number // 1000
number %= 1000

d2 = number // 100
number %= 100

d3 = number // 10
d4 = number % 10

print(d1, d2, d3, d4)

Output:
1 2 3 4

400. `==` checks equality.

`!=` checks inequality.

Floating-point calculations can have tiny precision errors.

print(0.1 + 0.2 == 0.3)

Output:
False

401.

a = 0.1 + 0.2
b = 0.3

if abs(a - b) < 0.00001:
print("Equal")

402.

print("apple" > "banana")

Output:
False

Strings are compared lexicographically using character Unicode values.

403.

print([1, 2, 3] > [1, 2, 2])

Output:
True

Python compares corresponding elements from left to right. The first difference is `3 > 2`.

404.

print((1, 2) == [1, 2])

Output:
False

A tuple and a list are different data types.

405.

year = int(input("Enter year: "))

if year % 400 == 0 or (year % 4 == 0 and year % 100 != 0):
print("Leap year")
else:
print("Not a leap year")

406.

a = float(input("Side 1: "))
b = float(input("Side 2: "))
c = float(input("Side 3: "))

if a + b > c and a + c > b and b + c > a:
print("Can form triangle")
else:
print("Cannot form triangle")

407.

print(True + True + False)

Output:
2

`True` acts like `1` and `False` acts like `0` in arithmetic.

408. Assuming `_` means `*`:

False * 100

Output:
0

True * 100

Output:
100

409.

x = 10
y = 20

print(x > 5 and y > 5)

Output:
True

410. Truth table:

and:
True and True → True
True and False → False
False and True → False
False and False → False

or:
True or True → True
True or False → True
False or True → True
False or False → False

not:
not True → False
not False → True

411.

print(5 and 10)

Output:
10

`and` returns an operand, not necessarily `True` or `False`. Since 5 is truthy, Python returns the second value, 10.

412.

print(0 and 10)
print(0 or 10)
print("" or "default")

Output:
0
10
default

413.

name = input("Enter name: ") or "Guest"

print(name)

If the user enters nothing, `"Guest"` is used.

414.

print(not 10 > 5)
print(not (10 > 5))

Both output:
False

The comparison happens before `not`.

415.

number = int(input("Enter number: "))

if 1 <= number <= 100 and number % 2 == 0 and number % 10 != 0:
print(True)
else:
print(False)

416. Yes, `+=` works with strings.

s = "hi"
s += " there"

print(s)

Output:
hi there

417. If the question means:

a = [1]
a_ = 3

This is valid Python. `a_` is simply a different variable name.

It does not change `a`.

418.

a = [1, 2]
b = a

b += [3]

print(a)

Output:
[1, 2, 3]

`b` and `a` refer to the same list, and `+=` modifies that list.

419.

a = [1, 2]
b = a

b = b + [3]

print(a)

Output:
[1, 2]

`b + [3]` creates a new list, then `b` points to that new list.

420. Q418 changes the original list because `+=` mutates the list.

Q419 creates a new list using `+`, so the original list remains unchanged.

421.

"a" in {"a": 1}

Output:
True

For a dictionary, `in` checks keys.

To check values:
1 in {"a": 1}.values()

Output:
True

422.

text = input("Enter text: ")
search = input("Search for: ")

if search.lower() in text.lower():
print("Found")
else:
print("Not found")

423.

a = 10
b = 10

print(a is b)

Often:
True

Python commonly interns/reuses small integers.

For larger integers:
a = 1000
b = 1000

`a is b` may be True or False depending on how the objects were created.

Use `==` for values.

424. Interning means Python may reuse the same object for certain values, such as some small integers and strings, to save memory.

Therefore `is` can sometimes appear to work for equal values, but it should not be used for ordinary value comparison.

425.

numbers = [1, 3, 5, 8, 9]

has_even = False

for number in numbers:
if number % 2 == 0:
has_even = True
break

print(has_even)

Output:
True

426.

print(~5)

Output:
-6

Bitwise NOT follows:
~x = -(x + 1)

So:
~5 = -6

427. `<<` shifts bits to the left.

print(5 << 1)

Output:
10

`>>` shifts bits to the right.

print(5 >> 1)

Output:
2

428. Bitwise operators can be used for flags and permissions.

Example:
READ = 1
WRITE = 2
EXECUTE = 4

permissions = READ | WRITE

429. A ternary operator is a short way to write a simple if/else.

age = 20

status = "adult" if age >= 18 else "minor"

print(status)

430.

score = 75

grade = "A" if score >= 70 else "B" if score >= 60 else "C" if score >= 50 else "F"

print(grade)

Output:
A

431. Yes.

x = 10 if True else 20

print(x)

Output:
10

432. `=` assigns a value.

`:=` assigns a value and also returns that value inside an expression.

Example:
if (x := 10) > 5:
print(x)

433.

while (text := input("Enter text: ")) != "quit":
print(text)

434.

a = 10
b = 25
c = 15

maximum = a if a > b and a > c else b if b > c else c

print(maximum)

Output:
25

435. Top operator precedence, from higher to lower:

436. `()`

437. `**`

438. Unary `+`, `-`, `~`

439. `*`, `/`, `//`, `%`

440. `+`, `-`

441. `<<`, `>>`

442. `&`

443. `^`

444. `|`

445. Comparisons: `<`, `>`, `<=`, `>=`, `==`, `!=`

446. Without parentheses:
     result = 10 + 5 * 2

Output:
20

With parentheses:
result = (10 + 5) * 2

Output:
30

437. `eval()` can execute Python code contained in a string, so using it with untrusted user input can be dangerous.

Example:
eval(user_input)

A safer alternative is to manually handle allowed operations.

438.

a = float(input("First number: "))
op = input("Operator (+, -, *, /): ")
b = float(input("Second number: "))

if op == "+":
result = a + b
elif op == "-":
result = a - b
elif op == "*":
result = a * b
elif op == "/":
if b != 0:
result = a / b
else:
result = "Cannot divide by zero"
else:
result = "Invalid operator"

print(result)

439.

m1 = float(input("Subject 1: "))
m2 = float(input("Subject 2: "))
m3 = float(input("Subject 3: "))
m4 = float(input("Subject 4: "))
m5 = float(input("Subject 5: "))

total = m1 + m2 + m3 + m4 + m5
average = total / 5
percentage = (total / 500) * 100

print("Total:", total)
print("Average:", average)
print("Percentage:", percentage)

440. The question's `_` means `*`:

celsius = float(input("Celsius: "))

fahrenheit = (celsius * 9 / 5) + 32

print(fahrenheit)

441.

number = float(input("Enter number: "))

if number > 0:
print("Positive")
elif number < 0:
print("Negative")
else:
print("Zero")

442.

ch = input("Enter a character: ")

if 'a' <= ch <= 'z':
print("Alphabet")
else:
print("Not a lowercase alphabet")

443.

balance = 10000
withdraw = 2000

if withdraw <= balance:
balance -= withdraw
print("Withdrawal successful")
print("Balance:", balance)
else:
print("Insufficient balance")

444.

password = input("Enter password: ")

has_length = len(password) >= 8
has_upper = any(ch.isupper() for ch in password)
has_digit = any(ch.isdigit() for ch in password)

if has_length and has_upper and has_digit:
print("Strong password")
else:
print("Weak password")

445.

print(bool(10 > 5 > 3 > 20))

Output:
False

Chained comparison means:

10 > 5 and 5 > 3 and 3 > 20

The last comparison is false.

446.

text = input("Enter text: ")

if len(text) > 5 and "a" in text and "z" not in text:
print(True)
else:
print(False)

447.

username = input("Username: ")
password = input("Password: ")

if username == "admin" and password == "1234":
print("Login successful")
else:
print("Invalid login")

448.

a = float(input("First number: "))
b = float(input("Second number: "))
c = float(input("Third number: "))

print("Addition:", a + b + c)
print("Subtraction:", a - b - c)
print("Multiplication:", a * b * c)

if b != 0 and c != 0:
print("Division:", a / b / c)

print("a == b:", a == b)
print("a != b:", a != b)
print("a > b:", a > b)
print("a < b:", a < b)
print("a >= b:", a >= b)
print("a <= b:", a <= b)

449.

price1 = float(input("Price of item 1: "))
qty1 = int(input("Quantity 1: "))

price2 = float(input("Price of item 2: "))
qty2 = int(input("Quantity 2: "))

price3 = float(input("Price of item 3: "))
qty3 = int(input("Quantity 3: "))

total = (price1 * qty1) + (price2 * qty2) + (price3 * qty3)

if total > 5000:
tax = total * 0.10
else:
tax = 0

final_total = total + tax

print("Subtotal:", total)
print("Tax:", tax)
print("Final total:", final_total)

450. Final Mega Project:

name = input("Enter student name: ")

score1 = float(input("Enter score 1: "))
score2 = float(input("Enter score 2: "))
score3 = float(input("Enter score 3: "))

average = (score1 + score2 + score3) / 3

top_students = ["John", "Mary", "David"]

if average >= 70:
grade = "A"
elif average >= 60:
grade = "B"
elif average >= 50:
grade = "C"
elif average >= 45:
grade = "D"
else:
grade = "F"

print("Name:", name)
print("Average:", average)
print("Grade:", grade)

if name in top_students:
print("Student is in the top students list")
else:
print("Student is not in the top students list")
